/*this is entry point which is going to
import as well as export all the modules

because we need to check entry point from package.json*/ 

const mypackage=require('./app');

module.exports=mypackage;

//now let us export it own our git account



